"use client";

import Link from "next/link";
import { LoaderCircle, MapPinned, PackageCheck, Plus, Settings2, UsersRound } from "lucide-react";
import { useEffect, useRef, useState } from "react";
import { StoreLocationPicker } from "@/components/maps/store-location-picker";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { StatusNote } from "@/components/ui/status-note";
import { formatINR } from "@/lib/utils";

type Operations = { restaurant: any; categories: any[]; orders: any[]; members: any[] };

export function AdminDashboard({ restaurantId }: { restaurantId: string }) {
  const [data, setData] = useState<Operations | null>(null);
  const [message, setMessage] = useState<string | null>(null);
  const [busy, setBusy] = useState(false);
  const [categoryName, setCategoryName] = useState("");
  const [item, setItem] = useState({ categoryId: "", name: "", price: "" });
  const [team, setTeam] = useState({ phone: "", role: "rider" });
  const settingsFormRef = useRef<HTMLFormElement>(null);
  async function load() { const response = await fetch(`/api/admin/restaurants/${restaurantId}`); const payload = await response.json(); if (!response.ok) throw new Error(payload.error ?? "Operations could not be loaded."); setData(payload); setItem((current) => ({ ...current, categoryId: current.categoryId || payload.categories[0]?.id || "" })); }
  useEffect(() => { void load().catch((error) => setMessage(error instanceof Error ? error.message : "Operations could not be loaded.")); }, [restaurantId]);
  async function saveSettings(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); if (!data) return; setBusy(true); const form = new FormData(event.currentTarget); const response = await fetch(`/api/admin/restaurants/${restaurantId}`, { method: "PUT", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ name: form.get("name"), addressText: form.get("addressText"), phone: form.get("phone") || null, description: form.get("description") || null, latitude: Number(form.get("latitude")), longitude: Number(form.get("longitude")), deliveryFeeBase: Number(form.get("deliveryFeeBase")), deliveryFeePerKm: Number(form.get("deliveryFeePerKm")), deliveryRadiusKm: Number(form.get("deliveryRadiusKm")), active: form.get("active") === "on" }) }); const payload = await response.json(); setBusy(false); if (!response.ok) { setMessage(payload.error); return; } setData({ ...data, restaurant: payload }); setMessage("Restaurant settings saved."); }
  async function createMenu(kind: "category" | "item") { const payload = kind === "category" ? { kind, name: categoryName } : { kind, categoryId: item.categoryId, name: item.name, price: Number(item.price), vegetarian: true }; const response = await fetch(`/api/admin/restaurants/${restaurantId}/menu`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(payload) }); const result = await response.json(); if (!response.ok) { setMessage(result.error); return; } setCategoryName(""); setItem({ categoryId: item.categoryId, name: "", price: "" }); await load(); }
  async function addMember(event: React.FormEvent) { event.preventDefault(); const response = await fetch(`/api/admin/restaurants/${restaurantId}/members`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(team) }); const result = await response.json(); if (!response.ok) { setMessage(result.error); return; } setTeam({ phone: "", role: "rider" }); setMessage("Team member added."); await load(); }
  async function updateOrder(id: string, status: string) { const response = await fetch(`/api/admin/orders/${id}`, { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ status }) }); const result = await response.json(); if (!response.ok) { setMessage(result.error); return; } await load(); }
  async function assignRider(orderId: string, riderId: string) { if (!riderId) return; const response = await fetch(`/api/admin/orders/${orderId}/assignment`, { method: "PUT", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ riderId }) }); const result = await response.json(); if (!response.ok) { setMessage(result.error); return; } setMessage("Rider assigned."); await load(); }
  if (!data) return <main className="grid min-h-screen place-items-center bg-cream"><LoaderCircle className="h-7 w-7 animate-spin text-saffron" /></main>;
  const restaurant = data.restaurant;
  const riders = data.members.filter((member) => member.role === "rider");
  return <main className="min-h-screen bg-cream"><header className="border-b border-orange-100 bg-white px-5 py-4"><div className="mx-auto flex max-w-6xl items-center justify-between"><Link href={`/restaurants/${restaurant.slug}`} className="font-bold">← {restaurant.name}</Link><span className="rounded-full bg-ink px-3 py-1 text-sm font-bold text-white">Operations</span></div></header><div className="mx-auto max-w-6xl px-5 py-8">{message && <div className="mb-5"><StatusNote tone={message.includes("saved") || message.includes("added") || message.includes("assigned") ? "success" : "error"}>{message}</StatusNote></div>}<div className="grid gap-6 lg:grid-cols-[1.25fr_.75fr]"><Card className="p-6"><div className="flex items-center gap-2"><Settings2 className="h-5 w-5 text-saffron" /><h1 className="display-font text-3xl">Restaurant settings</h1></div><form ref={settingsFormRef} className="mt-6 space-y-4" onSubmit={(event) => void saveSettings(event)}><div className="grid gap-3 sm:grid-cols-2"><Input name="name" defaultValue={restaurant.name} required /><Input name="phone" defaultValue={restaurant.phone ?? ""} placeholder="Contact phone" /></div><Input name="addressText" defaultValue={restaurant.address_text ?? ""} placeholder="Store address" required /><textarea name="description" defaultValue={restaurant.description ?? ""} className="min-h-20 w-full rounded-xl border border-stone-200 bg-white p-3 outline-none focus:ring-4 focus:ring-orange-100" placeholder="Restaurant description" /><div className="grid gap-3 sm:grid-cols-3"><Input name="deliveryFeeBase" type="number" min="0" step="0.01" defaultValue={restaurant.delivery_fee_base} aria-label="Base delivery fee" /><Input name="deliveryFeePerKm" type="number" min="0" step="0.01" defaultValue={restaurant.delivery_fee_per_km} aria-label="Fee per km" /><Input name="deliveryRadiusKm" type="number" min="0.1" step="0.1" defaultValue={restaurant.delivery_radius_km} aria-label="Delivery radius" /></div><StoreLocationPicker latitude={Number(restaurant.latitude)} longitude={Number(restaurant.longitude)} onChange={({ latitude, longitude }) => { const latitudeInput = settingsFormRef.current?.elements.namedItem("latitude") as HTMLInputElement | null; const longitudeInput = settingsFormRef.current?.elements.namedItem("longitude") as HTMLInputElement | null; if (latitudeInput) latitudeInput.value = String(latitude); if (longitudeInput) longitudeInput.value = String(longitude); }} /><div className="grid gap-3 sm:grid-cols-2"><Input name="latitude" type="number" step="0.000001" defaultValue={restaurant.latitude} /><Input name="longitude" type="number" step="0.000001" defaultValue={restaurant.longitude} /></div><label className="flex items-center gap-2 text-sm font-semibold"><input name="active" type="checkbox" defaultChecked={restaurant.active} />Accept orders</label><Button type="submit" disabled={busy}>{busy && <LoaderCircle className="h-4 w-4 animate-spin" />}Save restaurant settings</Button></form></Card><Card className="p-6"><div className="flex items-center gap-2"><UsersRound className="h-5 w-5 text-saffron" /><h2 className="display-font text-2xl">Team access</h2></div><p className="mt-2 text-sm text-stone-600">Members must sign in with their phone before being added.</p><form className="mt-5 space-y-3" onSubmit={(event) => void addMember(event)}><Input placeholder="Rider/manager mobile number" value={team.phone} onChange={(event) => setTeam({ ...team, phone: event.target.value })} /><select className="h-11 w-full rounded-xl border border-stone-200 bg-white px-3" value={team.role} onChange={(event) => setTeam({ ...team, role: event.target.value })}><option value="rider">Rider</option><option value="manager">Manager</option></select><Button size="sm" type="submit"><Plus className="h-4 w-4" />Add team member</Button></form><div className="mt-5 space-y-2">{data.members.map((member) => <div className="flex items-center justify-between rounded-xl bg-orange-50 px-3 py-2 text-sm" key={member.user_id}><span>{member.profiles?.display_name || member.profiles?.phone || member.user_id.slice(0, 8)}</span><span className="font-bold capitalize text-saffron">{member.role}</span></div>)}</div></Card></div><div className="mt-6 grid gap-6 lg:grid-cols-2"><Card className="p-6"><h2 className="display-font text-2xl">Menu studio</h2><div className="mt-5 grid gap-4 border-b border-orange-100 pb-5 sm:grid-cols-[1fr_auto]"><Input placeholder="New category, e.g. Burgers" value={categoryName} onChange={(event) => setCategoryName(event.target.value)} /><Button variant="secondary" disabled={!categoryName} onClick={() => void createMenu("category")}><Plus className="h-4 w-4" />Category</Button></div><div className="mt-5 grid gap-3 sm:grid-cols-2"><select className="h-12 rounded-xl border border-stone-200 bg-white px-3" value={item.categoryId} onChange={(event) => setItem({ ...item, categoryId: event.target.value })}>{data.categories.map((category) => <option key={category.id} value={category.id}>{category.name}</option>)}</select><Input placeholder="Item name" value={item.name} onChange={(event) => setItem({ ...item, name: event.target.value })} /><Input placeholder="Price in INR" type="number" min="0" value={item.price} onChange={(event) => setItem({ ...item, price: event.target.value })} /><Button variant="secondary" disabled={!item.categoryId || !item.name || !item.price} onClick={() => void createMenu("item")}><Plus className="h-4 w-4" />Menu item</Button></div><div className="mt-5 space-y-3">{data.categories.map((category) => <div key={category.id}><p className="font-bold">{category.name}</p><div className="mt-2 flex flex-wrap gap-2">{(category.menu_items ?? []).map((menuItem: any) => <span className="rounded-full bg-orange-50 px-3 py-1 text-sm" key={menuItem.id}>{menuItem.name} · {formatINR(Number(menuItem.price))}</span>)}</div></div>)}</div></Card><Card className="p-6"><div className="flex items-center gap-2"><PackageCheck className="h-5 w-5 text-saffron" /><h2 className="display-font text-2xl">Live orders</h2></div><div className="mt-5 space-y-3">{data.orders.map((order) => <div className="rounded-2xl border border-orange-100 p-4" key={order.id}><div className="flex items-center justify-between"><span className="font-mono text-xs text-stone-500">#{order.id.slice(0, 8)}</span><span className="font-bold">{formatINR(Number(order.total))}</span></div><p className="mt-2 text-sm font-semibold capitalize">{String(order.status).replaceAll("_", " ")} · {order.payment_method}</p>{riders.length > 0 && <label className="mt-3 block text-xs font-bold uppercase tracking-wide text-stone-500">Assign rider<select className="mt-1 h-9 w-full rounded-lg border border-stone-200 bg-white px-2 text-sm font-normal text-ink" defaultValue={order.delivery_assignments?.[0]?.rider_id ?? ""} onChange={(event) => void assignRider(order.id, event.target.value)}><option value="">Choose a rider</option>{riders.map((rider) => <option key={rider.user_id} value={rider.user_id}>{rider.profiles?.display_name || rider.profiles?.phone || rider.user_id.slice(0, 8)}</option>)}</select></label>}<div className="mt-3 flex flex-wrap gap-2">{nextStatuses(order.status).map((status) => <Button size="sm" variant="secondary" key={status} onClick={() => void updateOrder(order.id, status)}>{status.replaceAll("_", " ")}</Button>)}</div></div>)}{!data.orders.length && <p className="text-sm text-stone-600">Orders will appear here as soon as customers check out.</p>}</div></Card></div></div></main>;
}

function nextStatuses(status: string) { return ({ pending_approval: ["confirmed", "cancelled"], confirmed: ["preparing", "cancelled"], preparing: ["out_for_delivery", "cancelled"], out_for_delivery: ["delivered"], pending_payment: [], delivered: [], cancelled: [] } as Record<string, string[]>)[status] ?? []; }
